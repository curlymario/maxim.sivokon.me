import Typed from 'typed.js';

var options = {
  strings: ["интерфейсы", "веб-сайты", "приложения"],
  typeSpeed: 40,
  backSpeed: 0,
  backDelay: 500,
  startDelay: 1000,
  loop: true,
}

var typed = new Typed("#title-typed", options);